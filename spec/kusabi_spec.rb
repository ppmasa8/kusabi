# frozen_string_literal: true

RSpec.describe Kusabi do
  it "has a version number" do
    expect(Kusabi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
